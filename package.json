{
  "name": "natalis-persianas",
  "private": true,
  "description": "Landing page Nátalis Persianas — Cloudflare Pages",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "images": "node scripts/optimize-images.js",
    "build": "node scripts/verify.js",
    "dev": "wrangler pages dev public --port 8788",
    "deploy": "npm run build && wrangler pages deploy public --project-name=natalis-p --branch=main",
    "deploy:preview": "npm run build && wrangler pages deploy public --project-name=natalis-p"
  },
  "devDependencies": {
    "sharp": "^0.34.2",
    "wrangler": "^4.100.0"
  }
}
